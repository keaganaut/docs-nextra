import { Tree, File, Folder} from 'components/fileTree'

# Baz


<details>
<summary>Checklist</summary>

- [x] Create a space
- [ ] Add data source
- [ ] Transform data

</details>


<Tree>
  <Folder name="branch" defaultOpen>
    <Folder name="integrations">
        <File name="sources.yaml" />
    </Folder>
    <Folder name="models" defaultOpen >
        <File name="stg_orders.sql" />
        <File name="stg_customers.sql" />
        <File name="stg_payments.sql" />
    </Folder>
    <File name="manifest.json" />
  </Folder>
</Tree>

```sql filename="stg_customers.sql"

with source as (
    select * from {{ ref('stg_customers.sql') }}
)

select * from source
```

---

| Tables        | Are           | Cool  |
| ------------- |:-------------:| -----:|
| col 3 is      | right-aligned | $1600 |
| col 2 is      | centered      |   $12 |
| zebra stripes | are neat      |    $1 |

> This is a very long line that will still be quoted properly when it wraps. Oh boy let's keep writing to make sure this is long enough to actually wrap for everyone. Oh, you can *put* **Markdown** into a blockquote. 
